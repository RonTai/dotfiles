[user]
        email = email
        name = name
[color]
        ui = auto
        status = auto
        diff = auto
        branch = auto
        interactive = auto
        grep = auto
[merge]
        ff = false
[core]
        autocrlf = input
 
 
[push]
    default = tracking
[alias]
    co = checkout
    st = status
    ci = commit
    # lg = log --graph --date=iso --pretty='%C(yellow)%h%Creset %C(cyan bold)%d%Creset %s %Cgreen[%cd]%Creset' --decorate --all --stat
    lg = log --oneline --graph --date=iso --pretty='%C(blue)%an %C(yellow)%h %Creset %C(cyan bold)%d %C(bold green)[%cd] %C(bold green)(%ar) \n\t ⇛ %Creset %s %Creset' --decorate --all --stat
[http]
    sslVerify = false
